<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Now Playing - <%= song.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gradient-to-b from-black to-red-700 text-white min-h-screen">

    <header class="bg-red-900 p-4 shadow-md fixed top-0 left-0 right-0 z-10">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-white text-3xl font-bold">SpotiBri</h1>
            <nav class="mt-2">
                <a href="/songs" class="text-white hover:text-black-300 px-3 py-2 transition duration-200">Songs</a>
                <a href="/upload" class="text-white hover:text-black-300 px-3 py-2 transition duration-200">Upload Song</a>
            </nav>
    
    
            <div class="flex items-center space-x-4">
                <a href="#" class="rounded-full bg-red-500 text-white p-2 h-10 w-10 flex items-center justify-center hover:bg-red-600 transition duration-300">
                    <i class="fas fa-user"></i>
                </a>
            </div>
        </div>
    </header>

    <main class="container mx-auto flex-grow">
        <div class="flex flex-col items-center mt-24">
            <h2 class="text-4xl font-bold text-red-500 mb-4">Now Playing</h2>
            
            <div class="bg-black rounded-lg shadow-lg p-6 w-full max-w-2xl">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <img src="<%= albumArtUrl %>" alt="<%= song.album %> Cover Art" class="w-24 h-24 rounded-md">
                    </div>
                    <div class="ml-6">
                        <h3 class="text-2xl font-bold text-red-400"><%= song.title %></h3>
                        <p class="text-gray-300 mt-2">Artist: <%= song.artist %></p>
                        <p class="text-gray-400">Album: <%= song.album %></p>
                    </div>
                </div>

                <div class="mt-6 flex justify-center items-center space-x-4">
                    <a href="/play/<%= previousSongId %>" class="text-gray-400 hover:text-red-500">
                        <i class="fas fa-step-backward fa-2x"></i>
                    </a>

                    <audio controls class="w-full mt-2">
                        <source src="<%= song.song_url %>" type="audio/mpeg">
                        Your browser does not support the audio element.
                    </audio>

                    <a href="/play/<%= nextSongId %>" class="text-gray-400 hover:text-red-500">
                        <i class="fas fa-step-forward fa-2x"></i>
                    </a>
                </div>
            </div>
        </div>
    </main>

</body>

</html>
